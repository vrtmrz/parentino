# ((= greeting)), ((= name))

Author: ((= author.name)) - ((= author.role))

((define project "parentino")) Project: ((= project))

((if author.name "Author is set" "No author"))

Upper: ((upper name)) Lower: ((lower greeting))

Escaped: '((= name)) stays literal.
